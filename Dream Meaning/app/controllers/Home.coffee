'use strict'

angular.module('dreamMeaningApp')
  .controller 'HomeCtrl', ($scope, $timeout) ->
    # Bind controller data
    do ->

    # Attach listeners


    # Initialize controller
    do ->
      steroids.view.navigationBar.show { title: "Dreams Meaning" }

      steroids.view.navigationBar.show ' '
